{% extends '../layouts/dashboard' %}

{% set page_title = 'Login' %}
{% set body_class = 'login' %}

{% block content %}
  <div class="row">
    <div class="large-4 large-offset-4 columns">
      <div class="login-container">
        {{ form.open({ method: 'POST', route: 'dashboard.login' }) }}
          {{ csrfField }}

          <div class="row">
            <div class="large-12 columns">
              <label>
                Username <input type="text" name="username" />
              </label>
            </div>
          </div>

          <div class="row">
            <div class="large-12 columns">
              <label>
                Password <input type="password" name="password" />
              </label>
            </div>
          </div>

          <div class="row">
            <div class="large-12 columns actions">
              <button class="button expanded" type="submit">Login</button>
            </div>
          </div>
        {{ form.close() }}
      </div>
      <div class="login-credits">
        <small>SECV Server &bullet; STI College Novaliches</small>
      </div>
    </div>
  </div>
{% endblock %}
